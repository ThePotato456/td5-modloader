# Game symbol maps

Build-specific symbol maps belong here. Phase 3 will define the versioned map
schema and add the first supported BTD5 4.8 map. Maps must contain signatures,
offsets, and validation metadata only—never copied game code or assets.

